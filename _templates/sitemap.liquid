{% assign date_time_string = '{{ site.time | date_to_xmlschema }}' %}

{% for keys in software %}
 {% for l in keys[1] %}
<url>
  <loc>http://www.linuxalt.com/linux-software/{{l.internal_link}}</loc>
  <lastmod>{{ date_time_string }}</lastmod>
  <changefreq>hourly</changefreq>
</url>
 {% endfor %}
{% endfor %}

